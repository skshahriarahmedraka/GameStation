package controller_test

import (
	// "app/database"
	// "app/model"
	// "bytes"
	// "encoding/json"
	// "net/http"
	// "net/http/httptest"
	// "reflect"
	"testing"

	// "github.com/gin-gonic/gin"
	// "github.com/stretchr/testify/assert"
	// "go.mongodb.org/mongo-driver/mongo"
)

func TestDatabaseCollections_Login(t *testing.T) {
	//  CONNECT TO MONGODB
	// DB := database.MongodbConnection()
	// H := database.DatabaseInitialization(DB)

	// mockRes := `{"message":"successfully logged in"}`
	// // ROUTER 
	// r:= gin.Default()
	// r.POST("/api/login", H.Login())

	// loginData := model.LoginModel{}
	// loginData.Email = ""
	// loginData.Password = ""

	// SentLoginData, _ := json.Marshal(loginData)

	// req, _ := http.NewRequest("POST", "/api/login", bytes.NewBuffer(SentLoginData))
	// w := httptest.NewRecorder()
	// r.ServeHTTP(w, req)
	// resData:= w.Body.String()

	// assert.Equal(t, mockRes, resData)
	// assert.Equal(t, http.StatusOK, w.Code)

}
